export const API_PORT = 8080;
export const API_ENDPOINT = `http://localhost:${API_PORT}`;
export const API_WS_ENDPOINT = `ws://localhost:${API_PORT}`;
export const API_TIMEOUT = 1.5 * 60 * 1000;
export const PERPAGE: PerPage[] = [15, 30, 50];
