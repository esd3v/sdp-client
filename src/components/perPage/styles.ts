import {css} from 'emotion';

export const perPage = css`
  display: flex;
  align-items: center;
  padding: 1px;
  margin-top: 16px;
  & .label {
    margin-right: 8px;
  }
  & select {
    padding: 3px 8px;
  }
`;
